{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ghostwire.fullname" . }}
  labels:
    {{- include "ghostwire.labels" . | nindent 4 }}
  annotations:
    # WebSocket and VNC-specific annotations
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-buffer-size: "4k"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "600"
    {{- if .Values.haproxy.enabled }}
    nginx.ingress.kubernetes.io/websocket-services: "{{ include "ghostwire.fullname" . }}-haproxy"
    {{- else }}
    nginx.ingress.kubernetes.io/websocket-services: "{{ include "ghostwire.fullname" . }}"
    {{- end }}
    nginx.ingress.kubernetes.io/upstream-keepalive-timeout: "600"
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if $.Values.haproxy.enabled }}
                name: {{ include "ghostwire.fullname" $ }}-haproxy
                port:
                  number: 6901
                {{- else }}
                name: {{ include "ghostwire.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
                {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
