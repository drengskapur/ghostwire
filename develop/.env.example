# Flux GitOps Configuration
# Copy this file to .env and customize for your environment

# GitHub repository (format: owner/repo)
FLUX_GITHUB_REPO=drengskapur/ghostwire

# Git branch to track
FLUX_GITHUB_BRANCH=main

# Cluster name (used for naming conventions)
FLUX_CLUSTER_NAME=ghostwire

# Kubernetes context to use
FLUX_CLUSTER_CONTEXT=k3d-ghostwire

# GitHub deploy key title (must be unique per cluster)
FLUX_DEPLOY_KEY_TITLE=flux-k3d-ghostwire

# SSH key directory (defaults to ~/.ssh/flux-${FLUX_CLUSTER_NAME})
FLUX_SSH_KEY_DIR=${HOME}/.ssh/flux-ghostwire

# Path to Flux manifests in repository
FLUX_MANIFESTS_PATH=./develop

# Container Registry Credentials (optional - for private registries)
# Required if using private container images or Helm charts from GHCR
# DOCKER_USERNAME=your-github-username
# DOCKER_PASSWORD=your-github-token-or-password
